(library
  (public_name current)
  (libraries current_term lwt cmdliner sqlite3 lwt.unix)
  (preprocess (per_module
                ((pps ppx_deriving.std) level)
                (future_syntax current)
              )))
