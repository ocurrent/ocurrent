(library
  (public_name current_rpc)
  (name current_rpc)
  (libraries capnp capnp-rpc capnp-rpc-lwt stdint lwt lwt.unix logs result fmt fpath)
  (flags (:standard -w -53-55)))

(rule
 (targets schema.ml schema.mli)
 (deps schema.capnp)
 (action (run capnpc -o %{bin:capnpc-ocaml} %{deps})))
