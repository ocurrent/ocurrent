(executables
  (names docker_build_local build_matrix)
  (libraries current
             current_docker
             current_git
             current_fs
             lwt.unix
             logs.fmt
             fmt.tty)
  (preprocess future_syntax))
