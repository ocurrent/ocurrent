; (mdx
;  (deps (package current_git))
;  (package current_git))

(test
  (name git_test)
  (libraries alcotest
             alcotest-lwt
             current
             current_git
             lwt))
