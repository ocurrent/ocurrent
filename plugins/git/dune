(library
  (public_name current_git)
  (libraries current current.cache fpath lwt.unix irmin-watcher lwt fmt bos result astring logs ppx_deriving_yojson)
  (preprocess (per_module
                ((pps ppx_deriving_yojson) commit_id commit clone)
              )))
